<%= form_for(@order) do |f| %>
  <% if @order.errors.any? %>
    <td id="error_explanation">
      <h2><%= pluralize(@order.errors.count, "error") %> prohibited this order from being saved:</h2>

      <ul>
      <% @order.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </td>
  <% end %>
 <%= f.hidden_field :product_id, :value =>local_assigns[:locals][:product] %>

  <td class="field">
    <%= f.label :qty %><br>
    <%= f.text_field :qty %>
  </td>
  <td class="field">
    <%= f.label :order_date %><br>
    <%= f.date_select :order_date %>
  </td>
    
  <td class="actions">
    <%= f.submit %>
  </td>
<% end %>
