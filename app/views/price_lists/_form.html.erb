
<%= form_for [@price_list.supplier, @price_list] do |f| %>
  <% if @price_list.errors.any? %>
  <%= puts @price_list %>}
    <div id="error_explanation">
      <h2><%= pluralize(@price_list.errors.count, "error") %> prohibited this price_list from being saved:</h2>

      <ul>
      <% @price_list.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :"Effective Date" %><br>
    <%= f.date_select :EffectiveDate %>
  </div>
  <div class="field">
    <%= f.label :"Load CSV file"%><br>
    <%= file_field_tag :file %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
